{% extends "LibraryAppBundle::base.html.twig" %}

{% block body -%}
    <div class="row">
        <div class="col-md-12">
            <h2>Edit User</h2>
        </div>
    </div>

    <div class="container">
        <div class="col-md-8">
            {%  form_theme edit_form "LibraryAppBundle:Form:form_div_horizontal_layout.html.twig" %}
            {{ form_start(edit_form, {'attr': {'role': 'form', 'novalidate': 'novalidate'}}) }}
            {{- form_widget(edit_form) -}}
            {{ form_end(edit_form) }}
        </div>
        <div class="col-md-4">
            <p>
                {{ form(delete_form) }}
            </p>
        </div>
    </div>


</ul>
{% endblock %}
